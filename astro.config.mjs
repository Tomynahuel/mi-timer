import { defineConfig } from 'astro/config';

export default defineConfig({
  // Cambia TU_USUARIO por tu nombre de usuario en GitHub
  site: 'https://tomynahuel.github.io',
  // Cambia TU_REPOSITORIO por el nombre que le darás a tu repositorio en GitHub
  base: '/TU_REPOSITORIO',
});